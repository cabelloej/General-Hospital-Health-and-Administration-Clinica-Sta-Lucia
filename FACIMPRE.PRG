IF WSALIDA = 2
   DEFI WIND WINDSAL FROM 0,0 TO 25,81;
          NONE NOFLOAT NOZOOM NOGROW NOSHADOW COLOR SCHEME 10
   ACTI WIND WINDSAL
   STORE 21 TO WSALTO
ELSE
   SET DEVI TO PRINT
   STORE 60 TO WSALTO
ENDIF
*
STORE FACCXCTR.MONTO   TO WMONTOESC
STORE SPACE(1)         TO WESCRITO
DO MONTOESC
*
SELECT FACCXCTR
@ 01,00 SAY CHR(14)+QQWW
@ 02,60 SAY CHR(18)+"Fecha : "+DTOC(FACCXCTR.FECHA)
@ 03,60 SAY CHR(18)+"Cta.No: "+ALLTRIM(FACCXCTR.CODCXC)
@ 05,30 SAY CHR(14)+"RECIBO"
@ 06,28 SAY CHR(18)+"POR Bs."+STR(FACCXCTR.MONTO,9,2)
@ 10,00 SAY "   Hemos recibido de:"+WQNOMCLI
@ 12,00 SAY "la cantidad de "+wescrito
@ 14,00 SAY "por concepto de abono a la factura No."+substr(faccxc.referencia,4,7)+" de fecha "+dtoc(faccxc.elaborado)
@ 20,50 SAY "--------------------"
@ 22,50 SAY "   ADMINISTRACION   "
*
IF WSALIDA=2
   STORE "OPRIMA ENTER PARA FINALIZAR" TO WTEXT
   DO AVISO WITH WTEXT
   RELE WIND WINDSAL
ELSE
   EJECT
   SET DEVI TO SCRE
ENDIF

