STORE "OPCIONES: (I)NCLUIR, (A)CTUALIZAR, (S)ALIR" TO TEX
STORE "SIA" TO WCH
DO PREGUNTA
IF WCH="S"
   RETURN
ENDIF
IF WCH="I"
   SELECT COMDATA
   IF RECLOC()
      REPLACE COMSERICOT WITH COMSERICOT+1
      STORE COMSERICOT   TO   WNUMNUM
      FLUSH
      UNLOCK ALL
   ENDIF
ELSE
   STORE 0 TO WNUMNUM
ENDIF

STORE 0              TO WNUMERO
STORE "COT"          TO WTIPODOC
STORE "SOLICITUD "   TO WDOCDES1
STORE "   DE     "   TO WDOCDES2
STORE "COTIZACION"   TO WDOCDES3
STORE SPACE(10)      TO WNUMERO2
STORE SPACE(12)      TO WPROVEEDOR
STORE DATE()         TO WELABORADO
STORE 0              TO WMONTODOC
STORE .F.            TO WFLAGELA
DO COMELADC
IF .NOT. WFLAGELA
   RETURN
ENDIF
STORE "DESEA IMPRIMIR LA SOLICITUD ? (S/N)" TO TEX
STORE "SN" TO WCH
DO PREGUNTA
IF WCH = "S"
   DO WHILE .T.
      DO COMIMPDC
      STORE "CONFORME CON IMPRESION ? (S/N)" TO TEX
      STORE "SN" TO WCH
      DO PREGUNTA
      IF WCH="S"
         EXIT
      ENDIF
   ENDDO
ENDIF

