SELECT AFIACAD
DEFI WIND BRACA FROM LIBRACA,CIBRACA TO LIBRACA+10,CIBRACA+48 ;
                TITLE " NIVEL ACADEMICO PACIENTES " ;
                FOOTER " F1=Inc,F2=Mod,F3=Eli,F4=Bus,ESC=Sel ";
                DOUBLE NOFLOAT NOZOOM NOGROW SHADOW COLOR SCHEME 10
ACTI WIND BRACA
ON KEY LABEL F1 DO BRINC
ON KEY LABEL F2 DO BRMOD
ON KEY LABEL F3 DO BRELI
ON KEY LABEL F4 DO BRBUS
BROWSE FIELDS NIVACA:H="NIVEL ACADEMICO", DESCRI:H="DESCRIPCION";
              NOAPPEND NODELETE NOEDIT NOMENU NOOPTIMIZE REST SAVE IN WINDOW BRACA
ON KEY LABEL F1
ON KEY LABEL F2
ON KEY LABEL F3
ON KEY LABEL F4
RELEASE WIND BRACA
RETURN
***********************
***                 ***
***RUTINAS DE BRACA ***
***                 ***
***********************
PROCEDURE BRINC
STORE .T. TO WINC
DEFI WIND WINC FROM LIBRACA+5,CIBRACA+01 TO LIBRACA+09,CIBRACA+78;
               TITLE " INCLUIR " DOUBLE NOFLOAT NOZOOM NOGROW SHADOW COLOR SCHEME 10
ACTI WIND WINC
DO WHILE WINC
   SCAT MEMV BLANK
   @ 00,00 CLEAR
   @ 00,01 SAY "NIVEL:       DESCRIPCION:"
   @ 00,07 GET M.NIVACA
   READ
   IF LASTKEY()=27
      RELEASE WIND WINC
      EXIT
   ENDIF
   STORE M.NIVACA TO WCLAVEACA
   IF LEN(TRIM(WCLAVEACA))=0
      LOOP
   ENDIF
   SEEK WCLAVEACA
   IF FOUND()
      @ 00,28 SAY DESCRI
      STORE "ORGANIZACION-CLIENTE YA REGISTRADA, VERIFIQUE" TO WTEXT
      DO AVISO WITH WTEXT
      LOOP
   ELSE
      @ 00,28 GET M.DESCRI
      READ
      *** INICIO RUTINA DE ACEPTAR-CANCELAR
      store 1 to wop
      do while .t.
         @ 02,20  get wop pict "@*H Aceptar;Cancelar" defa wop
         read
         if lastkey()=13
            exit
         endif
      enddo
      *** FIN  RUTINA ACEPTAR-CANCELAR
      if wop=1
         if filloc()
            append blank
            gath memvar
            unlock all
         endif
      endif
   ENDIF
ENDDO
RETURN
***
PROCEDURE BRMOD
IF EOF()
   RETURN
ENDIF
DEFI WIND WMOD FROM LIBRACA+5,CIBRACA+01 TO LIBRACA+09,CIBRACA+78;
               TITLE " MODIFICAR " DOUBLE NOFLOAT NOZOOM NOGROW SHADOW COLOR SCHEME 10
ACTI WIND WMOD
SCAT MEMVAR
@ 00,01 SAY "NIVEL:       DESCRIPCION:"
@ 00,07 SAY M.NIVACA
@ 00,28 GET M.DESCRI
READ
*** INICIO RUTINA DE ACEPTAR-CANCELAR
store 1 to wop
do while .t.
   @ 02,20  get wop pict "@*H Aceptar;Cancelar" defa wop
   read
   if lastkey()=13
      exit
   endif
enddo
*** FIN  RUTINA ACEPTAR-CANCELAR
if wop=1
   if recloc()
      gath memvar
      unlock all
   endif
endif
RELEASE WIND WMOD
RETURN
***
PROCEDURE BRELI
DEFI WIND WELI FROM LIBRACA+5,CIBRACA+01 TO LIBRACA+09,CIBRACA+78;
               TITLE " ELIMINAR " DOUBLE NOFLOAT NOZOOM NOGROW SHADOW COLOR SCHEME 10
ACTI WIND WELI
SCAT MEMVAR
@ 00,01 SAY "NIVEL:       DESCRIPCION:"
@ 00,07 SAY M.NIVACA
@ 00,28 SAY M.DESCRI
*** INICIO RUTINA DE ACEPTAR-CANCELAR
store 1 to wop
do while .t.
   @ 02,20  get wop pict "@*H Aceptar;Cancelar" defa wop
   read
   if lastkey()=13
      exit
   endif
enddo
*** FIN  RUTINA ACEPTAR-CANCELAR
if wop=1
   if recloc()
      delete
      unlock all
   endif
endif
RELEASE WIND WELI
RETURN
***
PROCEDURE BRBUS
STORE .T. TO WBUS
DEFI WIND WBUS FROM LIBRACA+5,CIBRACA+01 TO LIBRACA+09,CIBRACA+78;
               TITLE " BUSCAR " DOUBLE NOFLOAT NOZOOM NOGROW SHADOW COLOR SCHEME 10
ACTI WIND WBUS
DO WHILE WBUS
   SCAT MEMV BLANK
   @ 00,01 SAY "NIVEL:       DESCRIPCION:"
   @ 00,07 GET M.NIVACA
   READ
   IF LASTKEY()=27
      RELEASE WIND WBUS
      EXIT
   ENDIF
   STORE M.NIVACA TO WCLAVEACA
   IF LEN(TRIM(WCLAVEACA))=0
      LOOP
   ENDIF
   SEEK WCLAVEACA
   IF FOUND()
      EXIT
   ELSE
      STORE "NO REGISTRADO, VERIFIQUE" TO WTEXT
      DO AVISO WITH WTEXT
   ENDIF
ENDDO
RELEASE WIND WBUS
RETURN
***
PROCEDURE BRORD
* SET ORDER TO ??
RETURN

