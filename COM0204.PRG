STORE "OPCIONES: (I)NCLUIR, (A)CTUALIZAR, (S)ALIR" TO TEX
STORE "SIA" TO WCH
DO PREGUNTA
IF WCH="S"
   RETURN
ENDIF
IF WCH="I"
   SELECT COMDATA
   IF RECLOC()
      REPLACE COMSERIDEV WITH COMSERIDEV+1
      STORE COMSERIDEV   TO   WNUMNUM
      FLUSH
      UNLOCK ALL
   ENDIF
ELSE
   STORE 0   TO WNUMNUM
ENDIF

STORE 0              TO WNUMERO
STORE "DEV"          TO WTIPODOC
STORE "DEVOLUCION"   TO WDOCDES1
STORE "   DE     "   TO WDOCDES2
STORE " COMPRAS  "   TO WDOCDES3
STORE SPACE(10)      TO WNUMERO2
STORE SPACE(12)      TO WPROVEEDOR
STORE DATE()         TO WELABORADO
STORE 0              TO WMONTODOC
STORE .F.            TO WFLAGELA
DO COMELADC
IF .NOT. WFLAGELA
   RETURN
ENDIF
STORE "DESEA IMPRIMIR LA DEVOLUCION ? (S/N)" TO TEX
STORE "SN" TO WCH
DO PREGUNTA
IF WCH = "S"
   DO WHILE .T.
      DO COMIMPDC
      STORE "CONFORME CON IMPRESION ? (S/N)" TO TEX
      STORE "SN" TO WCH
      DO PREGUNTA
      IF WCH="S"
         EXIT
      ENDIF
   ENDDO
ENDIF
RETURN
